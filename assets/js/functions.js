var lang = "en";

$(function () { // -----------------------------------------------------------------------

  $("[lang]").each(function (index, obj) {
    var $dit = $(obj);
    if (!$dit.is('div[lang="' + lang + '"]')) {
      $dit.hide();
    };
    console.log($dit);
  });

}); // ----------------------------------------------------------------------------------
