var lang = "en";

$(function () { // -----------------------------------------------------------------------

  $("[lang]").each(function (index, obj) {
    var $dit = $(obj);
    console.log($dit);
    if (!$dit.is('[lang="' + lang + '"]')) { 
      $dit.hide();
    };
  });

}); // ----------------------------------------------------------------------------------
